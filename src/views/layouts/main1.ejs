<!--<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Nodejs Passport Local</title>
</head>
<body>
  NAVIGATION 
  <% if (!user) { %>
  <a href="/login1">Signin</a>
  <a href="/signup1">Signup</a>
  <% } else { %>
  <a href="/">Logout</a>
  <a href="/profile">Profle</a>
  <% } %>

  <%if (signinMessage) {%>
    <p><%= signinMessage %></p>
  <% } %>
  <%if (signupMessage) {%>
    <p><%= signupMessage %></p>
  <% } %>

  <%- body %>
</body>
</html>*/-->